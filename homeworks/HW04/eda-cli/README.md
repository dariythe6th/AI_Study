# EDA & Data Quality Service

Пакет `eda-cli` предоставляет инструменты для автоматизированного **разведочного анализа данных (EDA)** и оценки **качества CSV-файлов**. Доступен как через командную строку (CLI), так и через полноценное HTTP API на базе FastAPI.

---

## Установка и настройка

1.  **Клонируйте репозиторий** и перейдите в его директорию.
2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv venv
    # Для Linux/macOS:
    source venv/bin/activate
    # Для Windows:
    venv\Scripts\activate
    ```
3.  **Установите пакет в режиме разработки:**
    ```bash
    pip install -e .
    ```

---

## Быстрый старт: HTTP API (FastAPI)

Сервис предоставляет REST API для анализа качества данных, напрямую использующее ядро `eda-cli`.

### Запуск сервера

```bash
uvicorn eda_cli.api:app --reload --port 8000
```
Сервис будет доступен по адресу: [http://127.0.0.1:8000](http://127.0.0.1:8000)

 **Автоматическая документация API (Swagger/ReDoc):**
- [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)
- [http://127.0.0.1:8000/redoc](http://127.0.0.1:8000/redoc)

### Ключевые эндпоинты

| Метод | Эндпоинт | Описание |
| :--- | :--- | :--- |
| `GET` | [`/health`](http://127.0.0.1:8000/health) | Проверка работоспособности сервиса. |
| `POST` | `/quality` | Расчет метрик качества на основе переданных числовых параметров (JSON). |
| `POST` | **`/quality-from-csv`** | **Основной анализ CSV-файла.** Возвращает полную сводку (`summarize_dataset`) и таблицу пропусков (`missing_table`). |
| `POST` | **`/quality-flags-from-csv`** | **Анализ флагов качества (HW03).** Возвращает словарь с флагами проблем данных (константные столбцы, высокая кардинальность и др.). |

### Особенности реализации (HW04)

*   **Интеграция с ядром:** Эндпоинты `/quality-from-csv` и `/quality-flags-from-csv` используют функции `core.summarize_dataset` и `core.compute_quality_flags`.
*   **Эвристики качества (HW03):** В анализ включены флаги `has_constant_columns` и `has_high_cardinality` (порог задаётся через `cardinality_threshold`).
*   **Надёжная обработка ошибок:** При попытке загрузить пустой, битый или файл неверного формата сервис возвращает понятную ошибку с кодом состояния **HTTP 400 Bad Request**.

---

## 🖥 Использование CLI (Командная строка)

Инструмент командной строки позволяет быстро проводить анализ локально и генерировать отчёты.

### Основные команды

```bash
# Быстрый обзор статистики в терминале
eda-cli overview <путь_к_csv_файлу>

# Генерация подробного HTML/JSON отчёта в директорию `reports/`
eda-cli report <путь_к_csv_файлу>
```

### Пример с расширенными флагами (HW03)

```bash
eda-cli report data/example.csv \
  --cardinality-threshold 20 \
  --json-summary
```
*   `--cardinality-threshold`: Задает порог для определения столбцов с высокой кардинальностью (флаг `has_high_cardinality`).
*   `--json-summary`: Дополнительно сохраняет сводку в формате JSON.

---

## Тестирование

Для запуска полного набора тестов (включая модульные тесты и тесты API) выполните:

```bash
python -m pytest
```

---

## Резюме изменений для HW04

1.  **Добавлен FastAPI сервер** (`eda_cli/api.py`).
2.  **Реализован эндпоинт загрузки файлов** (`/quality-from-csv`) с полноценной валидацией входных данных и обработкой ошибок (HTTP 400).
3.  **Интеграция эвристик HW03:** Флаги качества (кардинальность, константные столбцы) теперь включены в ответы API.
4.  **Добавлен специальный эндпоинт** (`/quality-flags-from-csv`) для получения только флагов качества.
