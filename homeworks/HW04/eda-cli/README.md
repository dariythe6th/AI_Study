# \# eda-cli: Инструмент для EDA и сервиса проверки качества данных

# 

# `eda-cli` — это мини-приложение, разработанное для быстрого анализа (EDA) и проверки качества данных в формате CSV. Проект представлен как инструмент командной строки (CLI) и как HTTP-сервис на FastAPI.

# 

# \## 🚀 1. CLI-инструмент (EDA и Отчёты)

# 

# Для работы с CLI необходимо установить проект в режиме разработчика (`pip install -e .`) в активированном виртуальном окружении.

# 

# \### 1.1. Доступные команды CLI

# 

# | Команда | Описание |

# | :--- | :--- |

# | `overview` | Выводит краткую сводку по датасету: размеры, типы данных, пропуски и количество уникальных значений по колонкам. |

# | `report` | Генерирует полный аналитический отчёт, включая файлы CSV, графики (`.png`) и основной отчёт в Markdown (`report.md`) с интегральной оценкой качества. |

# 

# \### 1.2. Новые параметры команды `report` (HW03)

# 

# В команду `report` были добавлены параметры для управления процессом анализа и активации дополнительного режима вывода:

# 

# | Параметр | Тип | По умолчанию | Влияние на отчёт |

# | :--- | :--- | :--- | :--- |

# | `--report-title` | `str` | `"EDA-отчёт"` | Заголовок H1 в `report.md`. |

# | `--cardinality-threshold` | `int` | `50` | Порог, при превышении которого активируется флаг качества \*\*`has\_high\_cardinality`\*\* (для нечисловых колонок). |

# | `--max-cat-columns` | `int` | `5` | Максимальное число категориальных колонок, для которых будут сгенерированы таблицы Top-K. |

# | `--json-summary` | `bool` | `False` | Если установлен, генерирует компактную сводку ключевых метрик в файл \*\*`summary.json`\*\*. |

# 

# \### 1.3. Пример вызова CLI

# 

# ```bash

# \# Генерация полного отчёта с использованием новых опций

# eda-cli report data/example.csv \\

# &nbsp;   --out-dir analysis\_results \\

# &nbsp;   --report-title "Отчет по датасету User\_Metrics" \\

# &nbsp;   --cardinality-threshold 10 \\

# &nbsp;   --json-summary

